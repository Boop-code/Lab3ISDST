Text Search App (Вариант 17)
Краткое описание проекта

Приложение рекурсивно сканирует указанную директорию, ищет заданное ключевое слово в файлах с указанными расширениями и выводит абсолютные пути файлов, где слово было найдено.

    Цель: автоматизация поиска логов, ошибок или ключевых фраз в больших наборах текстовых файлов.
    Реализовано на: Java 17, Gradle (сборка и управление зависимостями)
    Особенности:
        Поддержка регистрозависимого и регистронезависимого поиска
        Гибкая настройка через app.properties
        100% покрытие бизнес-логики unit-тестами
        Кроссплатформенная сборка (Windows, Linux)

Пример работы
Вход (файл app.properties):

properties
1
2
3
4

Содержимое папки ./data:

1
2
3
4

Выход (stdout при запуске):

1
2

    Каждый файл выводится только один раз, даже если совпадений несколько.

Как выполнить сборку проекта

    Убедитесь, что установлен JDK 17+
    В корне проекта выполните:

bash
1
2
3
4
5

    После успешной сборки исполняемый JAR будет создан:

    1

    Сборка включает: компиляцию, запуск тестов и создание JAR-артефакта.

Как выполнить конфигурацию проекта

Создайте файл app.properties в той же папке, где находится JAR-файл (он не включён в JAR и игнорируется в Git).
Поддерживаемые параметры:
Параметр
	
Описание
	
Пример
scan.directory
	
Директория для рекурсивного сканирования
	
./logs
search.keyword
	
Обязательно! Ключевое слово для поиска
	
CRITICAL
search.case.sensitive
	
Учитывать регистр (true/false)
	
false
scan.file.extensions
	
Расширения файлов через запятую (с точкой!)
	
.txt,.log,.out
Пример корректного app.properties:

properties
1
2
3
4

    ⚠️ Важно: файл должен находиться рядом с JAR, а не в src/main/resources.

Как запустить тесты и какой результат ожидать

Выполните в корне проекта:

bash
1
2

Ожидаемый результат:

    Все тесты проходят успешно (BUILD SUCCESSFUL)
    Покрыты следующие компоненты:
        FileScanner — рекурсивный обход и фильтрация по расширению
        TextSearcher — поиск с учётом/игнорированием регистра
        AppConfig — создание конфигурации (через фабричный метод)

Отчёт о тестах:
build/reports/tests/test/index.html
Как запустить приложение

    Убедитесь, что:
        Существует файл app.properties рядом с JAR
        Указана директория scan.directory и она существует
        В ней есть файлы с расширениями из scan.file.extensions
    Запустите:

bash
1

    Приложение выведет в консоль абсолютные пути всех файлов, содержащих search.keyword.

    💡 Пример вывода:
    D:\projects\text-search-app\data\app.log
